# 前端本地开发覆盖项。复制为 frontend/.env.local 后按需修改。
#
# 这个文件与根目录 .env 刻意分开：根目录 .env 存放数据库口令和 BOT API 凭证，
# Vite 不读取该文件，避免任何后端机密进入前端构建链路。
#
# 端口默认值见 vite.config.ts，一律使用 "BOI + 100" 偏移，
# 避免与 BOI 系统的 5173 / 4173 / 8000 冲突。

# ZWT_DEV_PORT=5273
# ZWT_PREVIEW_PORT=4273
# ZWT_API_TARGET=http://127.0.0.1:8100
